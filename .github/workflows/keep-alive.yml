name: Keep Alive

on:
  schedule:
    # Setiap Minggu jam 12:00 UTC (19:00 WIB)
    - cron: '0 12 * * 0'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Keep repo active
        run: echo "Keep alive at $(date -u)"
